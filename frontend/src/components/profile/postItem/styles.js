import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    flex: 1 / 3,
    height: 200,
    backgroundColor: 'gray'
  },
  image: {
    flex: 1
  }
});